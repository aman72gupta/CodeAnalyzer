The project has 6 main components- Analyzer, CommandLineParser, Display, Executive, FileManager and Parser
Executive is the pivot point which operates other components

There is a batch file named "Run", user can give filepaths and options in there
Default lookup directory- ../../../
option x- get results in XML format
option s- look in sub directories as well
option r- show relations among classes